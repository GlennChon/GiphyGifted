import giphyService from "../src/services/giphyService";

// mock function
// const add = jest.fn(()=>3);

// expect add function to be called once.
// expect(add).toHaveBeenCalledTimes(1);

// expect add function to be called with 1 and 3 as inputs
// expect(add).toHaveBeenCalledWith(1,3);

/*
export async function getGifs(searchValue, offset) {
    try {
      let response = await fetch(searchUrl(searchValue, offset));
      if (!response.ok) {
        throw response;
      } else {
        return response.json();
      }
    } catch (err) {
      alert(err);
    }
  }
  
  export async function getTrendingGifs(offset) {
    try {
      let response = await fetch(trendingUrl(offset));
      if (!response.ok) {
        throw response;
      } else {
        return response.json();
      }
    } catch (err) {
      alert(err);
    }
  }
  
  export async function getRandomGif() {
    try {
      let response = await fetch(randomUrl());
      if (!response.ok) {
        throw response;
      } else {
        return response.json();
      }
    } catch (err) {
      alert(err);
    }
    */
